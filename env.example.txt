# Configurações do Sistema Contábil
# Copie este arquivo para .env e configure os valores

# Ambiente
ENVIRONMENT=production
DEBUG=False

# Banco de Dados PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://contabil_user:SUA_SENHA_AQUI@localhost:5432/contabil_db

# Segurança
SECRET_KEY=GERE_UMA_CHAVE_SECRETA_ALEATORIA_AQUI

# Streamlit
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=127.0.0.1
STREAMLIT_SERVER_HEADLESS=true

# Logging
LOG_LEVEL=INFO
LOG_DIR=/var/log/contabil

# Backup
BACKUP_DIR=/var/backups/contabil/postgresql
BACKUP_RETENTION_DAYS=7

# Email (opcional - para notificações)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu_email@gmail.com
# SMTP_PASSWORD=sua_senha_app
# SMTP_FROM=noreply@seudominio.com

# Domínio
DOMAIN=seudominio.com
ALLOWED_HOSTS=seudominio.com,www.seudominio.com

# PostgreSQL (para scripts de backup)
POSTGRES_DB=contabil_db
POSTGRES_USER=contabil_user
POSTGRES_PASSWORD=SUA_SENHA_AQUI
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# IA (opcional - se usar configuração fixa)
# AI_FIXED_PROVIDER=openai
# AI_FIXED_MODEL=gpt-4o-mini
# AI_FIXED_API_KEY=sua_chave_api_aqui
# AI_FIXED_CONFIG_ENABLED=true

























