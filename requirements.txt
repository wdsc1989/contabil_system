# Core Framework
streamlit>=1.29.0
streamlit-authenticator>=0.2.3

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0  # Driver PostgreSQL para produção

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
xlrd>=2.0.0

# File Parsing
PyPDF2>=3.0.0
pdfplumber>=0.10.0
ofxparse>=0.21

# PDF to Image Conversion (para Vision API)
PyMuPDF>=1.23.0  # Principal: funciona no Linux sem poppler, recomendado para produção
pdf2image>=1.16.3  # Fallback: requer poppler-utils (instalado pelo setup_vps_hostinger.sh)

# OCR (Optical Character Recognition) - Opcional (para fallback se necessário)
pytesseract>=0.3.10  # OCR engine wrapper (opcional)
easyocr>=1.7.0  # OCR alternativo (opcional, para fallback)

# Image Processing
Pillow>=10.0.0  # Necessário para processamento de imagens

# Visualization
plotly>=5.18.0
altair>=5.2.0

# Export/Reporting
reportlab>=4.0.0

# Security
bcrypt>=4.1.0
pyyaml>=6.0.0

# Utilities
python-dateutil>=2.8.0
validators>=0.22.0
python-dotenv>=1.0.0  # Para carregar variáveis de ambiente do .env

# AI/ML - Vision API e outros provedores
openai>=1.0.0  # OBRIGATÓRIO: Para OpenAI GPT-4o Vision API
google-generativeai>=0.3.0  # Para Google Gemini (opcional)
groq>=0.4.0  # Para Groq (opcional)

# Development (opcional, pode remover em produção)
pytest>=7.4.0
faker>=21.0.0


